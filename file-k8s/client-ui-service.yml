apiVersion: v1
kind: Service
metadata:
  name: client-ui-service
  namespace: m-ecommerce
  labels:
    app: client-ui-service
spec:
  selector:
    app: client-ui-deployment
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000